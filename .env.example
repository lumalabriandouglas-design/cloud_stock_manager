# Copy to .env for local development
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Leave empty for SQLite locally. On Railway this is set automatically.
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Optional – for AI ledger scanner
GEMINI_API_KEY=

# Optional – real email later (Gmail, Resend, etc.)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# DEFAULT_FROM_EMAIL=noreply@yourdomain.com
